<!--
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.youyuan.springcloud.dao.DeptDao">

    <select id="get" resultType="Dept" parameterType="Integer">
        select dept_id,dept_name,db_source from dept where dept_id=#{dept_id};
    </select>
    <select id="list" resultType="Dept">
        select dept_id,dept_name,db_source from dept;
    </select>
    <insert id="add" parameterType="Dept">
        INSERT INTO dept(dept_name,db_source) VALUES(#{dept_name},DATABASE());
    </insert>

</mapper>


-->
